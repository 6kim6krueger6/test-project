## 📋 Требования

### Runtime
- **Bun** >= 1.3.8 - https://bun.sh  
  
  Bun - это быстрый всё-в-одном JavaScript runtime, созданный как альтернатива Node.js. Для установки:  
  
  ```bash
  curl -fsSL https://bun.sh/install | bash
  ```

### База данных
- **MariaDB** >= 10.5 или **MySQL** >= 5.7

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
bun install
```

### 2. Конфигурация переменных окружения

Создайте файл `.env` в корне проекта с необходимыми переменными:

```env
NODE_ENV=production
APP_PORT=8080

DATABASE_URL="mysql://user:password@localhost:3306/database_name?schema=public"
DB_HOST=xxxxx
DB_USER=xxx
DB_PORT=xxxx
DB_PASSWORD=xxx
DB_NAME=xxx

JWT_SECRET=xxxx
JWT_EXPIRATION=xxxx

```



### 3. Инициализация базы данных

```bash
# Создание таблиц на основе схемы Prisma
bun x prisma migrate deploy

# Генерация Prisma клиента
bun x prisma generate
```

### 4. Запуск приложения

```bash
# Развитие
bun run src/index.ts

# Или используя npm script
bun run start
```

Сервер запустится на `http://localhost:3000`

## 📁 Структура проекта

```
.
├── src/                      # Исходный код приложения
│   ├── index.ts             # Входная точка приложения
│   ├── routes/              # API маршруты
│   ├── controllers/          # Бизнес-логика
│   ├── middleware/           # Express middleware
│   ├── services/            # Бизнес-сервисы
│   └── utils/               # Утилиты
├── prisma/                   # Prisma ORM конфигурация
│   ├── schema.prisma        # Определение моделей БД
│   └── migrations/          # История миграций БД
├── uploads/                  # Загруженные файлы (создается автоматически)
├── postman_collection.json   # Postman коллекция для тестирования API
├── prisma.config.ts         # Конфигурация Prisma
├── tsconfig.json            # Конфигурация TypeScript
├── package.json             # Зависимости проекта
└── bun.lock                 # Lock файл для воспроизводимости

```

## 📦 Зависимости

### Production

| Пакет | Версия | Назначение |
|---|---|---|
| `express` | ^5.2.1 | Web фреймворк |
| `@prisma/client` | ^7.3.0 | ORM для работы с БД |
| `@prisma/adapter-mariadb` | ^7.3.0 | Адаптер для MariaDB |
| `jsonwebtoken` | ^9.0.3 | JWT аутентификация |
| `bcryptjs` | ^3.0.3 | Хеширование паролей |
| `multer` | ^2.0.2 | Загрузка файлов |
| `cookie-parser` | ^1.4.7 | Парсинг cookies |

### Development

| Пакет | Версия | Назначение |
|---|---|---|
| `@types/bun` | latest | Type definitions для Bun |
| `@types/express` | ^5.0.6 | Type definitions для Express |
| `typescript` | ^5 | Поддержка TypeScript |

## 🔐 Аутентификация

Проект использует JWT (JSON Web Tokens) для аутентификации:

1. **Регистрация** - пользователь передает email и пароль
2. **Вход** - сервер возвращает JWT токен
3. **Защита маршрутов** - токен передается в заголовке `Authorization: Bearer <token>`

## 📤 Загрузка файлов

Для загрузки файлов используется `multer`:

- Максимальный размер файла контролируется переменной `MAX_FILE_SIZE`
- Загруженные файлы сохраняются в директорию `UPLOAD_DIR`
- По умолчанию: 50MB в папке `./uploads`

## 📝 Postman коллекция

В проекте включена коллекция для тестирования API - `postman_collection.json`

### Импорт в Postman

1. Откройте Postman
2. Нажмите **Import**
3. Выберите **Upload Files**
4. Выберите файл `postman_collection.json`
5. Все эндпоинты и переменные окружения автоматически загружатся

### Переменные окружения в Postman

В коллекции используются следующие переменные:

```
{{baseUrl}}      - http://localhost:3000
{{token}}        - JWT токен (устанавливается после логина)
{{userId}}       - ID пользователя
```

## 🗄️ База данных

### Миграции

```bash
# Создать новую миграцию
bun x prisma migrate dev --name migration_name

# Применить миграции
bun x prisma migrate deploy

# Просмотр статуса миграций
bun x prisma migrate status

# Откат к предыдущей миграции
bun x prisma migrate resolve --rolled-back migration_name
```

### Seeding (Заполнение данных)

Если в проекте есть seed скрипт:

```bash
bun x prisma db seed
```

### Prisma Studio

Открыть интерактивный UI для управления БД:

```bash
bun x prisma studio
```

## 🧪 Тестирование

```bash
# Запуск тестов (если настроены)
bun test
```

## 🔧 Development команды

```bash
# Установка зависимостей
bun install

# Запуск приложения
bun run start

# Просмотр файлов проекта
bun run src/index.ts

# Синхронизация схемы Prisma с БД
bun x prisma db push

# Сброс БД (внимание: удалит все данные!)
bun x prisma db reset

# Изучить Prisma CLI
bun x prisma --help
```


## 📚 Полезные ссылки

- [Bun документация](https://bun.sh/docs)
- [Express.js документация](https://expressjs.com)
- [Prisma документация](https://www.prisma.io/docs)
- [JWT введение](https://jwt.io)
- [Postman документация](https://learning.postman.com)

## 📄 Лицензия

Проект создан с использованием `bun init` в Bun v1.3.8
